[package]
name = "transport_poc"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
boltffi = { path = "../../boltffi" }
boltffi_core = { path = "../../boltffi_core" }

[profile.release]
debug = 1
